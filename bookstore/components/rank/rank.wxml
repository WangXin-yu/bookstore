<view wx:for="{{_rank}}" wx:key="index" bindtap="goToRankDetail" data-_id="{{rank_id_array[index]}}">
  <view wx:if="{{index < 6}}" class="rank-item"  >
    <view >
      <image src="https://statics.zhuishushenqi.com{{item.cover}}" class="rank-icon"></image>
    </view>  
    <view>{{item.title}}</view>
  </view>
</view>